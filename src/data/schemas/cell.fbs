// Schema file for Cell objects

include "common.fbs";
include "point.fbs";

namespace schemas;

table Cell
{
    position:Vec3;
    corners:[Point] (required);
    children:[Cell];
    owned_vertices:byte; // TODO: Make this obsolete
}

root_type Cell;