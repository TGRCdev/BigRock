Import('env', 'sources')
testops = Variables()

sources += Glob("shared/*.cpp")

progsrc = Glob("*.cpp")
for prog in progsrc:
    env.Program(target='bin/' + str(prog)[:-4], source=sources + [prog])